<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="wedding_planner.list">
      <field name="name">WeddingPlanner list</field>
      <field name="model">wedding_planner.wedding_planner</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="wedding_planner.action_window">
      <field name="name">WeddingPlanner window</field>
      <field name="res_model">wedding_planner.wedding_planner</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="wedding_planner.action_server">
      <field name="name">WeddingPlanner server</field>
      <field name="model_id" ref="model_wedding_planner_wedding_planner"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="WeddingPlanner" id="wedding_planner.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="wedding_planner.menu_1" parent="wedding_planner.menu_root"/>
    <menuitem name="Menu 2" id="wedding_planner.menu_2" parent="wedding_planner.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="wedding_planner.menu_1_list" parent="wedding_planner.menu_1"
              action="wedding_planner.action_window"/>
    <menuitem name="Server to list" id="wedding_planner" parent="wedding_planner.menu_2"
              action="wedding_planner.action_server"/>
    -->
  </data>
</odoo>